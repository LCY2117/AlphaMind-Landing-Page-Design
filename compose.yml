services:
  alphamind:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: alphamind
    restart: unless-stopped
    env_file:
      - .env.local
    environment:
      HOST: 0.0.0.0
      PORT: 3001
    ports:
      - "127.0.0.1:3001:3001"
